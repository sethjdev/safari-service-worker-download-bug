{
  "name": "safari-service-worker-download-bug",
  "version": "1.0.0",
  "description": "Streaming downloads through a ServiceWorker work perfectly on macOS, but on iOS are frequently interrupted and result in incomplete files being saved to the user's device.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node https-server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.1.0"
  }
}
